import { HashLoader } from "react-spinners";

export default function () {
  return (
    <div className="h-full w-full flex items-center justify-center">
      <HashLoader color="#18acac" size="100px" speedMultiplier={1} />
    </div>
  );
}
